{
  "name": "cricket-career-lab",
  "version": "0.1.0",
  "private": true,
  "description": "Offline-first cricket training and career planning tools.",
  "type": "module",
  "bin": {
    "cricket-career-lab": "dist/cli/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/**/*.test.js",
    "check": "npm test",
    "cli": "npm run build --silent && node dist/cli/main.js",
    "serve": "npm run build --silent && node dist/http/main.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "typescript": "5.7.2"
  }
}
